**QRust - Generate QR Codes from URLs**

QRust is a simple and work-in-progress tool designed to effortlessly create QR codes from URLs.

To run, clone this repo and execute the following command:

```shell
cargo run <URL>
```